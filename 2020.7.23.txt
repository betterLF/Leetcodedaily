443. 压缩字符串
定义pre记录当前字符串值，curCount记录pre字符串出现次数，curIndex记录当前位置下标
遍历字符串，如果当前字符值与pre不同，就将当前curCount加入字符数组中，然后重置pre，curCount和curIndex，否则就curCount++，遍历结束如果curCount!=1，就继续进行一次，最后返回nowIndex值
447. 回旋镖的数量
外层遍历二维数组，定义map集合，key存放距离，value存放个数。里层在遍历一次二维数组，然后对该map的values进行遍历，大于等于2就排列组合加到结果集中，最后遍历结束得到结果
453. 最小移动次数使数组元素相等
数学方法:对n-1个数相加等价于对剩下的一个数-1，所以找到数组最小值，在让数组每个数-最小值即可。
排序:先对数组进行从小到大排序，然后最小值和最大值相等，要移动max-min次，此时最小值为0和n-1，n-2最大，此时在移动n[n-2]-n[0]次，使得三个元素相等，依次类推
455. 分发饼干
先对两个数组进行排序，然后双指针遍历两个数组，如果g[i]<=s[j]，则结果++，i++，j++,否则j++，最后只要有一个到达数组末尾退出循环返回结果