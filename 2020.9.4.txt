706. 设计哈希映射
哈希链表数组存放值，取模10000，然后模相同的放在一个链表里，进行操作即可。
717. 1比特与2比特字符
遍历数组，如果当前值为1则i++，然后无论如何都i++，如果能遍历到最后一个元素返回true，否则false
720. 词典中最长的单词
1:前缀树dfs
2:hashset加搜索
先对数组进行字典排序，然后进行遍历，如果当前字符串长度为1或者set集合中包含当前字符的前n-1个字符，则集合中加入该点，如果此时ans长度小于当前字符长度，更新ans。最后遍历结束返回结果。