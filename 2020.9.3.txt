703. 数据流中的第K大元素
定义优先权队列，大小为k，然后将数组中所有元素加入队列中，add方法中如果当前优先权队列长度小于目标长度，则直接加入该值，否则如果当前值大于队列第一个元素，第一个元素弹出，再添加。最后返回队列第一个元素。
704. 二分查找
定义l和r分别是第一，最后一个下标，然后while循环，如果l<=r就进行循环，根据情况进行l=mid+1，r=mid-1，或者返回即可。
705. 设计哈希集合
定义一个boolean数组，add将该位置置为true，remove将其置为false。contains返回该位置即可。