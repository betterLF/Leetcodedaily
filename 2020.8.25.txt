592. 分数加减运算
用队列存放数字和运算符，然后进行while遍历，只要数字运算符队列的长度不等于2，就取出前4个数和运算符第一个数进行运算，得到的结果进行约分(辗转相除)得到结果加入队列中继续进行即可。
593. 有效的正方形
先对坐标进行排序，按照左上，右上，左下，右下的顺序，然后判断四条边和两条对角线是否对应相等即可605. 种花问题
如果开头为0，则出现两个0就n--，然后开始只要0的个数为3，则n--，，并且重置0的个数为1，遍历到结尾时如果0的个数为2，则n--，最后返回n<=2。