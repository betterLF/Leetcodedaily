794. 有效的井字游戏
遍历字符串，统计X和O的个数，如果两字符串不等并且O的个数不比X小1，返回false；如果O和X的数目相等并且X能连成三点，返回false，如果O的数量比X少一，并且O能连成三点返回false。最后返回true。
795. 区间子数组个数
记录出所有值都小于等于R的子数组个数和所有值都小于等于L-1的子数组个数，两者相减就是结果。
797. 所有可能的路径
用栈dfs遍历即可。