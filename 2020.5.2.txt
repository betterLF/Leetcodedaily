406. 根据身高重建队列
先把数组按照升高降序排列（升高相同按照人数升序排列）
然后建立linkedList遍历加入数组，加入位置为比当前身高高的人数
714. 买卖股票的最佳时机含手续费
cash表示当我们不持有股票时的最大利润，
hold表示当我们持有股票时的最大利润。
cash = Math.max(cash, hold + prices[i] - fee);
hold = Math.max(hold, cash - prices[i]);
通过状态方程动态更新
944. 删列造序
纵向遍历二维数组即可，每一列只要有一个大于前面的元素结果集就++
1111. 有效括号的嵌套深度
其实就是一个栈的思想，栈中左括号的个数就代表深度
所以要想深度最小，相邻的两个左括号要分在不同的组别
按照奇偶性分类左括号