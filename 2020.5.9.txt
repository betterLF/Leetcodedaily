1222. 可以攻击国王的皇后
将皇后放入二维数组中
根据国王位置朝8个方向依次寻找
面试题 01.08. 零矩阵
hashset记录出现0的行号和列号
然后遍历行列hashset，赋值为0
1287. 有序数组中出现次数超过25%的元素
定义长度为数组的四分之一，因为数组有序，所以遍历数组
如果arr[i]=arr[i+len]，则说明其为答案
输出
面试题 17.10. 主要元素
因为主要元素大于一半，定义sum=1
如果下一个元素和当前存放元素不同，sum--；
如果sum==0就更换当前存放元素
因为主要元素大于一半，说以主要元素的sum最大，不可能减到0
所以遍历数组后剩下的一定是主要元素
但可能数组没有主要元素
所以要进行判断，遍历数组，查看数组中该元素值个数是否满足条件