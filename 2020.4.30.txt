136. 只出现一次的数字
异或操作，两个相同的数字异或操作后为0，所以对数组异或后就是出现一次的数字
811. 子域名访问计数
Hashmap，key网站名，value存放出现次数，动态更新
1002. 查找常用字符
新建集合数组，集合中有26个数组，每个数组长度为字符串数组的长度，每个数组的每个下标代表第几个字符串某个字符出现的次数，最后把26个字符排序，取出每个数组最小的
1207. 独一无二的出现次数
先Hashmap，key存放数字，value存放出现次数
然后Hashset将value值都加入，如果最终hashset的大小和数组大小一样，就返回true，否则false。