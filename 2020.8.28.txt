637. 二叉树的层平均值
层次遍历二叉树，记录每层的结点和和个数即可得到结果。
643. 子数组最大平均数 I
滑动窗口，先计算前k个元素之和，然后将其设置为最大值，然后进行向后遍历，更新当前和为当前值-前第k个值即可。更新最大值，最后返回最大值/k。
645. 错误的集合
遍历集合，每次遍历对下标+1和对应值和当前异或值(初始为0)相异或，遍历结束得到的值为重复数字和缺失数字之和。遍历期间用hashmap或者数组找到重复元素，最后返回重复元素，异或值异或重复元素即可。