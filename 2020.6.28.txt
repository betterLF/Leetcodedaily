263. 丑数
如果该数为0直接返回fasle，否则只要能被5整除，就一直除5，能被3整除，就一直除3，能被2整除就一直除2，然后判断是否为1，为1返回true，否则false
278. 第一个错误的版本
二分查找即可。注意有两种方式，第二种while条件是left<right，然后如果为true，right=mid,否则left=mid-1；最后然后left
290. 单词规律
用长度为27的String数组存放对应关系，遍历pattern，如果当前位置对应的数组值不为null，则将其与当前str对应单词对比，不同返回false，如果数组值为null，则查询对应单词是否出现在hashset中，出现代表已经和其他元素有对应关系了，返回false，否则将该点赋值，并将该单词加入set中。
292. Nim 游戏
如果该数可以被4整除，则你无论走几步，另外一个人补到4步就会赢。如果不可以被4整除，你第一步只要走了余数步，后面另外一个人走几步，你补到4步就能赢。