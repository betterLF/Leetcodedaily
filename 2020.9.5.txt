744. 寻找比目标字母大的最小字母
遍历数组，如果当前元素比目标字符大直接返回，否则遍历结束返回第一个元素。
724. 寻找数组的中心索引
遍历数组得到数组总和
然后遍历数组记录当前遍历元素之前的数组和，如果其*2=总和-当前元素，则返回当前下标，否则当前数组和加上该元素，继续遍历。最后返回-1。
剑指 Offer 20. 表示数值的字符串
模拟进行，首先表示数值的字符串只能出现两个数字，记录.和e和数字出现的位置即可。