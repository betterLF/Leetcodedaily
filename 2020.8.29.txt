653. 两数之和 IV - 输入 BST
中序遍历，每次遍历一个元素存放入set集合中，如果set中有值和其之和为k，返回true。
661. 图片平滑器
依次遍历各个点的邻接点并求值即可。
665. 非递减数列
while循环，只要数组不到末尾并且此时改变元素小于2就一直遍历，如果当前元素大于后一位元素，如果此时位置为0，直接改变当前元素，count++，否则如果当前元素下一个元素大于等于当前元素前一个元素，count++，代表修改当前元素，否则下一个元素值改为当前元素值，count++，最后返回count<2。