518. 零钱兑换 II
动态规划，对应每种零钱，从coin到amount进行遍历，dp[i]+=dp[i-coin]即可。最后返回dp[amount]。
519. 随机翻转矩阵
hashset存放已经变为1的所有点，flip函数，在0到sum之间随机产生一个数，如果该数未存在于set中，将其值加入set，并返回对应下标。reset函数直接将set清空即可。
522. 最长特殊序列 II
先对字符串数组按照字符串长度从大到小排序，然后从头开始遍历，如果当前字符串并不是其他所有字符串的子串，则直接返回该长度，否则继续遍历。