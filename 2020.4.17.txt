1030. 距离顺序排列矩阵单元格
方法一：数组排序（按照自定义的比较方法）
方法二：桶排序，求出最大距离，然后创建一个ArrayList<ArrayList<int[]>> res=new ArrayList<>();
每个ArrayList<int[]>存放对应的距离的坐标
242. 有效的字母异位词
直接将String转成字符数组，再进行内置函数排序，最后比较每位是否相同，注意先判断长度，不等直接返回