521. 最长特殊序列 Ⅰ
如果两字符串长度不等，就返回长度最大值
如果两字符串长度相等，循环比较每个字符
只要有一个不等，，就返回长度
否则返回-1
791. 自定义字符串排序
新建长度为26的int数组index，计数T中每个元素出现个数，新建stringbuilder
然后遍历S字符串，如果index数组中对应字符串的值不为0，就append该字符串，同时值减一
结束S的遍历T中字符串就已经按照规定有序了，剩下的就依次append即可
929. 独特的电子邮件地址
新建hashset
核心： ans.add(cur[0].split("\\+")[0].replace(".","")+"@"+cur[1]);
即把@前的+后面的字符串忽略，.去掉，然后加上@后的内容
最终ans的大小就是结果
12. 整数转罗马数字
新建hashmap，key为对应数值，value为对应罗马数字，再新建一个一维数组，按照数值从小到大排序
如果num-数值下标为index的元素>=0,结果就append该数值对应的罗马数字，且num减去该数组
如果小于该元素，index就--