840. 矩阵中的幻方
有数学知识可知，中心元素一定是5，这可以跳过很多情况，然后暴力求解即可。
849. 到最近的人的最大距离
从前往后和从后往前遍历两次数组，得到空位距离最近的1的最小距离，然后得到该种空位的最大值即可。
859. 亲密字符串
如果两字符串长度不等，直接false，如果两字符串相等，则进行判断某一个字符中是否有一个字符出行次数大于等于2，如果存在返回true，否则false。如果不等，则进行遍历字符串，如果不等的字符不为2则返回false，否则判断交换的字符是否相等，根据该结果进行返回。