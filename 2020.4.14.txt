1342. 将数字变成 0 的操作次数
 (num & 1) == 0 判断二进制数最后一位是否为1，即判断奇偶
num >> 1代表二进制数右移一位，即值除二
num & -2 代表二进制数与-2相与，即值减一（-2的二进制除最后一位为0，其余为1）
面试题 16.01. 交换数字
numbers[0] ^= numbers[1];//求两个数的异或和
numbers[1] ^= numbers[0];//对和进行异或得到相反结果
numbers[0] ^= numbers[1];
461. 汉明距离
无问题