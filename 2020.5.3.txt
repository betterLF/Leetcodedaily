122. 买卖股票的最佳时机 II
只要当前股票价格高于自己买入的就卖出当前股票
无论结果如何，买入的股票变成当前的股票
860. 柠檬水找零
如果给5元，5元数量加一
给10元，5元数量够，5就--，10++
给20元，优先-10-15，其次-5-5-5
1029. 两地调度
先按照两地费用差值从大到小排序数组
然后在遍历数组，选择AB两地
1094. 拼车
优先权队列存放行程，最前面的元素是下车地点最近的，如果下一个元素的上车位置<该地点，下一个元素就加入队列
否则就把队列中的元素取出来。载客量动态变化
1338. 数组大小减半
新建数组，下标表示元素值，值表示出现次数
将不为0的元素防止数组前部，对这些不为0的元素进行排序
然后从后往前遍历这些元素，当总和大于一半时就退出