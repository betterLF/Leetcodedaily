526. 优美的排列
回溯算法，如果当前遍历到的位置为N+1，直接ans++，返回。否则遍历集合，如果当前集合里的值满足该条件，递归回溯该点即可。
528. 按权重随机选择
前缀和+二分搜索:先用数组存放前缀和以及一个int值存放总和，然后随机产生0-总和之间的值，再到前缀和数组进行二分搜索得到结果。
529. 扫雷游戏
递归二维数组即可，如果是M，则直接更新该点退出，否则对该点的邻接点进行遍历，记录M的个数，如果最后个数为0，更新该点为B，进行下一层遍历，否则更新该点为M的个数。(用长度为8的二维数组表示可能的8个邻接点与当前点的位置关系，则判断邻接点情况可以直接for循环遍历)