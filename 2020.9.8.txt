806. 写字符串需要的行数
逐行统计到目前数组遍历的字符长度，如果超过100，就行++，最后返回行数和此时宽度。
812. 最大三角形面积
套用鞋套公式直接根据坐标计算三角形面积即可，三层for循环更新最大值。
819. 最常见的单词
遍历字符数组，记录当前单词出现的首位置，并且碰到大写字母就转成小写，如国碰到非字母，就对其进行转换，得到当前字符串，更新其在map集合里的大小，根据大小判断是否要更新结果，然后首位置重置，继续遍历。最后结束后还要遍历一次，防止到最后都没有出现非字母，导致最后一个字母没进行判断，记得在加入map前判断是否不在禁止数组中即可。