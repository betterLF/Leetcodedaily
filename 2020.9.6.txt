746. 使用最小花费爬楼梯
动态规划，当前元素值由前两个元素的最小值加上当前元素构成，最后返回末尾两个元素最小值。
747. 至少是其他数字两倍的最大数
遍历数组找到最大元素和其下标，然后再次遍历数组，如果当前值不为最大值并且其值两倍大于最大值返回-1，否则返回最大值下标。
748. 最短完整词
先得到当前车牌号字符出现次数的hash数组，然后遍历数组，如果当前字符串对应的字符数组中每一个值都大于等于hash数组的对应值，则当前字符符合条件，这时根据长度大小选择是否更新结果即可。