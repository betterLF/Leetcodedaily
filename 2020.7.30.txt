438. 找到字符串中所有字母异位词
滑动窗口:定义两个数组ss和pp,pp存放目标子串每个元素出现的次数
定义left=0,right=0滑动窗口，只要right小于数组长度，当前right位置所对应的s字符串值其个数在ss中+1，并且right右移，如果当前该字符的个数大于目标子串，则ss数组右移，即left位置元素个数--，left++。如果当前窗口大小等于目标数组大小，则匹配成功，将left加入结果集中。最后输出结果。
445. 两数相加 II
先将两个字符串进行翻转，然后相加，最后得到的链表再进行翻转得到结果
449. 序列化和反序列化二叉搜索树
序列化二叉树直接前序遍历，并且每个结点值之前用","隔开。
反序列化时将该字符串转成数组，然后当前开始位置为当前根节点，则找到第一个大于该点值的对应结点下标，从其开始就是其右子树，前面是左子树，这样就可以进行分治递归求解。