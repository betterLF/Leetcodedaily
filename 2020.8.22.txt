542. 01 矩阵
将所有的0的位置加入队列中，并将所有1转成-1表示未被访问，遍历队列，只要队列不为空，就弹出队首元素，就得到当前已经得到结果的值，然后对它四周未被访问过的点进行更新即可，更新后该点也加入队列。(此时距离已经是最近的，因为加入队列的顺序是按照从近到远排序的)
547. 朋友圈
dfs遍历，如果该行未被遍历，结果++，递归遍历该行，把与该行的所有有关行都遍历并依次递归并设置为已经访问过即可。
553. 最优除法
由数学知识可知，最优除法是由第一个数/后面所有数相除的结果。